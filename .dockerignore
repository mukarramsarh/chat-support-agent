# Keep secrets and local state out of the build context / image layers.
# At runtime the app reads .env from the bind mount instead.
.git
.env
vendor
storage/logs/*
storage/uploads/*
node_modules
*.md
